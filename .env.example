# ── Lokalni PostgreSQL ────────────────────────────────────────────────
# Format: postgresql://KORISNIK:LOZINKA@HOST:PORT/BAZA
DATABASE_URL="postgresql://postgres:LOZINKA@localhost:5432/graviranje"

# ── Better Auth ───────────────────────────────────────────────────────
# Generisi: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
BETTER_AUTH_SECRET="zameni-me"
BETTER_AUTH_URL="http://localhost:3000"

# ── Resend (magic link email) ─────────────────────────────────────────
RESEND_API_KEY="re_..."
FROM_EMAIL="onboarding@resend.dev"

# ── App ───────────────────────────────────────────────────────────────
NEXT_PUBLIC_APP_URL="http://localhost:3000"
